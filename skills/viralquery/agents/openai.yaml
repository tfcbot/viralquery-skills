interface:
  display_name: "ViralQuery"
  short_description: "Set up and run private video research"
  default_prompt: "Use $viralquery to verify my API setup, configure my research brief and rules, run a scroll, and return the strongest source videos and signals."
