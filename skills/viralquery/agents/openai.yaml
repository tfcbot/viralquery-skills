interface:
  display_name: "ViralQuery"
  short_description: "Build a private video research feed"
  default_prompt: "Use $viralquery to set my research brief, save my rules, run a bounded scroll, and return the strongest source videos and signals."
